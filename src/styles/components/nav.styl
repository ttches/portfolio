@import 'nib';
@import '../vars.styl';

nav
  background-color $nav-background-color
  color $nav-color
  padding 20px
  .nav-links
    display flex
    margin 0 auto
    width 80%
    .nav-link
      margin-right 20px
      user-select none
      &:hover
        cursor default
